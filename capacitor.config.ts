import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.chuckfact.jokes',
  appName: 'chuckStorage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
